# Chapter 03 — Phase 6: Narration Script (FROZEN)

- **Chapter ID:** `chapter-03-across-the-internet`
- **Parent artifacts:** `05_SCENE_DESIGN.md` (FROZEN) and all earlier frozen artifacts
- **Produced artifact:** full bilingual narration script (beat lines + UI labels + entity glosses)
- **Review status:** Passed — reviewed against `03_NARRATIVE_FRAMING`
- **Frozen status:** FROZEN — this exact text ships in `narration/beats.ts`, `narration/labels.ts` and the entity glosses

Laws enforced: observation before explanation (`§3`) · question before answer (`§4`) · terminology only after the phenomenon — PST sequence Question → Problem → Need → Solution → Terminology (`§13`, `AUTHORING_WORKFLOW` Phase 6) · no checklist/documentation tone, no forbidden patterns ("X is a component that…", "X is responsible for…") (`§14`) · neutral, plain, dual-audience language (`§10`, `§15`) · core terms locked to English, VI prose natural and written from scratch (`§15`).

---

## 1. Beat lines (EN / VI) — the exact strings

| Beat | EN | VI |
|---|---|---|
| b0 ready | Far away, a Server has finished preparing your page: the HTML file, one long stream of bytes. Watch what happens the moment it leaves the building. | Ở nơi xa, Server đã chuẩn bị xong trang của bạn: tệp HTML, một dòng bytes dài. Hãy nhìn khoảnh khắc nó rời khỏi máy. |
| b1 road | These bytes cannot travel in a straight line. There is no wire running from the Server all the way to you. Between the two machines lies a web of other computers, each one linked to a few neighbors. Your bytes must cross them one by one. Each of those machines is a Router, and the whole web is the Internet. | Đống bytes này không thể đi thẳng. Chẳng có sợi dây nào chạy từ Server tới tận chỗ bạn cả. Giữa hai cỗ máy là một mạng lưới những máy tính khác, máy nào cũng nối với vài máy hàng xóm. Bytes của bạn phải băng qua từng máy một. Mỗi máy như vậy là một Router, và cả mạng lưới đó là Internet. |
| b2 whole | Why not just send the file whole? Picture it: the file hogs the entire cable while everyone else waits. And one bad bit is enough to spoil the whole stream, which would have to go all the way back and start again. | Sao không gửi nguyên cả tệp cho rồi? Thử tưởng tượng: tệp chiếm trọn sợi cáp trong khi tất cả mọi người phải chờ. Và chỉ một bit hỏng là đủ làm hỏng cả dòng, phải quay về tận gốc và đi lại từ đầu. |
| b3 slice | So the Server never sends it whole. It slices the file into small pieces: Packets. Every packet carries one slice of the file, plus a header on top: where it comes from, where it is going, that same number DNS once handed over, and its place in line, the Sequence Number. | Nên Server không bao giờ gửi nguyên tệp. Nó cắt tệp thành những mảnh nhỏ: các Packet. Mỗi packet chở một lát của tệp, kèm một cái header ở trên: từ đâu tới, đi tới đâu, chính là con số DNS từng đưa, và vị trí trong hàng của nó, gọi là Sequence Number. |
| b4 route | At each Router, the packet is read for one thing only: where it is going. The Router hands it to the neighbor closest to that place, and the next Router does the same, and the next. Watch packet two: at one crossing it is sent a different way, because the usual cable is busy. Every crossing decides for itself, right now, from what it can see. That is Routing. | Ở mỗi Router, packet chỉ bị đọc đúng một thứ: nó đang đi đâu. Router trao nó cho người hàng xóm gần đích nhất, Router kế tiếp lại làm y hệt, rồi kế tiếp nữa. Nhìn packet số hai xem: ở một ngã rẽ nó được gửi đi đường khác, vì sợi cáp quen đang bận. Mỗi ngã rẽ tự quyết định, ngay lúc này, từ những gì nó nhìn thấy. Đó là Routing. |
| b5 mess | The packets reach your computer through the NIC. But look at the order: 1, 3, 5, 2. Number 4 never showed up. Somewhere on the road, a busy Router had to let it go. The Internet does not promise order, and it does not promise delivery. So, who puts this mess back into a file? | Các packet về tới máy của bạn qua NIC. Nhưng nhìn thứ tự xem: 1, 3, 5, 2. Số 4 chẳng thấy đâu. Ở đâu đó trên đường, một Router bận rộn đành phải bỏ rơi nó. Internet không hứa hẹn thứ tự, cũng không hứa hẹn giao đủ. Vậy, ai xếp đống lộn xộn này trở lại thành một tệp? |
| b6 slot-1 | Inside your machine, someone is watching for the pieces. Each one gets a numbered slot: piece 1 settles into slot 1. This keeper has a name, TCP. And this bench, where the pieces wait to be put together, is its Reassembly Buffer. | Bên trong máy của bạn, có người đang canh chừng từng mảnh. Mỗi mảnh có một ô đánh số: mảnh 1 yên vị vào ô 1. Người giữ trật tự này có tên, TCP. Còn chiếc bàn này, nơi các mảnh chờ được ghép lại, là Reassembly Buffer của nó. |
| b7 park | Piece 3 arrives next, but slot 2 is still empty. Does TCP throw it away? No. It parks 3 in its own slot, and 5 does the same. Out-of-order pieces are not errors. They simply wait in their numbered places. | Mảnh 3 tới tiếp, nhưng ô số 2 vẫn trống. TCP có vứt nó đi không? Không. Nó xếp mảnh 3 vào đúng ô của nó, và mảnh 5 cũng vậy. Những mảnh đến sai thứ tự không phải là lỗi. Chúng chỉ đơn giản là chờ ở đúng chỗ có đánh số. |
| b8 counter | Piece 2 lands, and watch the counter: it jumps from 1 straight to 3, because 1, 2, 3 are all here, in order. This little confirmation is the ACK: "I have everything up to here." It only ever moves forward, in order, and that is exactly how the other side learns what is missing. | Mảnh 2 đáp xuống, và nhìn bộ đếm nhé: nó nhảy từ 1 lên thẳng 3, vì 1, 2, 3 đã có đủ, đúng thứ tự. Lời xác nhận nhỏ này là ACK: "tôi đã có đủ tới đây." Nó chỉ tiến về phía trước, đúng thứ tự, và chính nhờ vậy mà đầu bên kia biết được mảnh nào đang thiếu. |
| b9 ask | So TCP asks for exactly one thing: "send piece 4 again." Not the whole file, just the missing piece. The Server hears it, and piece 4 crosses the road a second time. Slot 4 fills, the counter jumps to 5: everything is here. | Nên TCP chỉ xin đúng một thứ: "gửi lại mảnh 4." Không phải cả tệp, chỉ mảnh còn thiếu. Server nghe thấy, và mảnh 4 băng qua con đường lần thứ hai. Ô số 4 đầy, bộ đếm nhảy lên 5: đủ cả rồi. |
| b10 merge | Now the keeper reads the slots in order, 1, 2, 3, 4, 5, and stitches the slices back together. One stream of bytes, whole again: the HTML file, exactly as the Server sent it. | Giờ người giữ trật tự đọc các ô theo thứ tự, 1, 2, 3, 4, 5, và khâu các lát lại với nhau. Một dòng bytes duy nhất, nguyên vẹn trở lại: tệp HTML, đúng y như Server đã gửi đi. |
| b11 home | And the whole file settles into RAM, the fast working memory where every download waits. Not a byte missing, not a piece out of place. This is the exact hand-off you already know: the whole file in RAM, ready for the Browser Engine. | Và cả tệp trọn vẹn nằm vào RAM, bộ nhớ làm việc tốc độ cao nơi mọi thứ tải xuống nằm chờ. Không thiếu một byte, không mảnh nào lệch chỗ. Đây chính là khoảnh khắc chuyển giao bạn đã biết: tệp nguyên vẹn trong RAM, sẵn sàng cho Browser Engine. |
| b12 recap | So this is how data travels from the Server to the Browser. The file is sliced into Packets, each one stamped with a header and a Sequence Number. The Packets cross the Internet Router by Router, each choosing its own way. They arrive out of order, one is lost. TCP keeps them in numbered slots, asks only for the missing piece, and stitches everything whole, until the complete file rests in RAM. | Vậy đó là cách dữ liệu đi từ Server tới Browser. Tệp được cắt thành các Packet, mỗi packet được đóng dấu header và Sequence Number. Các Packet băng qua Internet từng Router một, mỗi packet tự chọn đường. Chúng đến sai thứ tự, một packet bị thất lạc. TCP giữ chúng trong những ô đánh số, chỉ xin lại đúng mảnh thiếu, và khâu tất cả lại cho nguyên vẹn, cho tới khi tệp hoàn chỉnh nằm gọn trong RAM. |

## 2. UI labels (labels.ts)
- chapterTitle: "How does data travel from Server to Browser?" / "Dữ liệu đi từ Server đến Browser như thế nào?"
- waitingLine: "The Server is ready with your page. Click it to send the answer home." / "Server đã sẵn sàng với trang của bạn. Bấm vào đó để gửi câu trả lời về nhà."
- startButton: "Send the response" / "Gửi response về"
- sceneTag (bench): "Inside the receiving machine" / "Bên trong cỗ máy nhận"
- tip: "Click the Server to begin. Use the < > buttons or the dots to step." / "Bấm vào Server để bắt đầu. Dùng nút < > hoặc các chấm để lùi/tới."
- doneLabel: "Done" / "Xong"

## 3. Glosses (side panel; EN / VI) — one per entity, shown only while the entity is active in a beat

- **Server**: The computer that answered your Browser. Right now it holds your page, and when a piece goes missing, it sends that piece again. / Cỗ máy đã trả lời Browser của bạn. Lúc này nó giữ trang của bạn, và khi một mảnh thất lạc, nó gửi lại đúng mảnh đó.
- **Router**: A computer whose whole job is passing things along. It reads where a packet is headed, then hands it to the neighbor closest to that place. It keeps nothing: every packet moves on in a blink. / Một máy tính mà cả công việc là chuyển tiếp. Nó đọc xem packet đang đi đâu, rồi trao cho người hàng xóm gần đích nhất. Nó không giữ lại gì: mọi packet đi tiếp trong chớp mắt.
- **Browser**: The main character of the last journey. Right now it simply waits: its page is still on the road. / Nhân vật chính của hành trình trước. Lúc này nó chỉ đơn giản là chờ: trang của nó vẫn còn trên đường.
- **NIC**: Your computer's door to the network; you met it before. Every byte that enters lands here first. / Cánh cửa ra mạng của máy tính; bạn đã gặp nó rồi. Mọi byte đi vào đều hạ cánh ở đây trước tiên.
- **RAM**: Fast working memory from Chapter 1. The finished file waits here, ready for the Browser. / Bộ nhớ làm việc nhanh từ Chapter 1. Tệp hoàn chỉnh chờ ở đây, sẵn sàng cho Browser.
- **CPU**: The CPU runs everything you are watching here, the Browser, TCP, the whole machine, one instruction at a time, just like Chapter 1. / CPU chạy mọi thứ bạn đang thấy ở đây, Browser, TCP, cả cỗ máy, từng lệnh một, đúng như Chapter 1.
- **GPU**: The GPU is resting: the page is not drawn yet. Its work belongs to the last journey. / GPU đang nghỉ: trang vẫn chưa được vẽ. Việc của nó thuộc về hành trình trước.
- **Reassembly Buffer**: TCP's bench: a numbered slot for every piece, where the file waits to be stitched whole. / Chiếc bàn của TCP: mỗi mảnh một ô đánh số, nơi tệp chờ được khâu lại nguyên vẹn.
- **NIC Port**: The door seen from inside, the same NIC. Pieces come in through it, and the ACKs leave through it. / Cánh cửa nhìn từ bên trong, chính là NIC đó. Các mảnh đi vào qua nó, và các ACK đi ra qua nó.

## 4. Self-check against `03` (quick record)
- **PST order honored per milestone:** b1 (Q "how does it leave?" → P "no wire" → N "shared web" → S "web lights up" → T "Router/Internet") · b2–b3 (P "whole file fails" → N "slice" → S "packets" → T "Packet/header/Sequence Number") · b4 (Q "how does each piece find the way?" → S "hop by hop" → T "Routing") · b5–b6 (Q "who fixes the mess?" → S "numbered slots" → T "TCP/Reassembly Buffer") · b8 (T "ACK" only after the counter is seen moving) · b9 (retransmission observed, no new jargon needed). ✓
- **No term before its observation** → see Phase 5 §4 matrix. ✓
- **No standalone component description:** every gloss appears only while its entity is mid-action; no "X is a component that…" pattern anywhere (grep-level). ✓
- **Misconceptions actively named:** M1 (b1 "there is no wire…"), M2 (b2/b3), M3 (b4/b5 "look at the order"), M4 (b7 "does TCP throw it away? No"), M5 (b9 "not the whole file"), M6 (b1 "a web of other computers"; b4 "every crossing decides for itself"). ✓
- **Central question kept open** until b11 (serial partial answers: distance → packets → routing → disorder → keeper → whole), visibly answered at b11, recited at b12. ✓
- **Backward references only** (Chapter 1, "the last journey"): rule A-01 (no naming of future chapters) honored; this chapter never names a successor. ✓
- **Terminology lock (VI):** Router, Internet, Packet, header, Sequence Number, NIC, TCP, Reassembly Buffer, ACK, Server, Browser, RAM, CPU, GPU, HTML, bytes, DNS, Browser Engine kept in English inside natural Vietnamese prose; straight quotes; no machine translation. ✓
