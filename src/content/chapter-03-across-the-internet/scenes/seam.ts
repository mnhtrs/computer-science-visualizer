// scenes/seam.ts — the two seam positions the engine holds the spark at while
// fading between scenes (exported separately so assemble.ts stays clean).

import { NIC_POS } from './01-the-wire'
import { RAM_DOOR } from './02-reassembly'

export { NIC_POS }
export const RAM_DOOR_POS = RAM_DOOR
