// rendering/parts/payload-renderer.ts
// The protagonist. A thin re-export so the payload is addressable as a
// "part renderer" alongside storage/processor/memory in the registry.

export { drawSpark as drawPayload } from '../primitives/spark'
